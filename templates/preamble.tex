% Optional extra LaTeX packages/macros for maintainers, injected via
% Pandoc's --include-in-header before \begin{document}. Empty by default;
% templates/book.tex already provides the packages required for tables,
% figures, header/footer, hyperlinks, and fonts.
