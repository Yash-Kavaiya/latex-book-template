# Introduction

This chapter is maintained as Markdown in `notes/`.  Add later chapters with a
numeric prefix (for example, `02-methods.md`) and list them in `book.yaml`.

```mermaid
flowchart LR
  Markdown --> Pandoc --> PDF
```

