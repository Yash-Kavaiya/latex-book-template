# Maintainer filters

The Mermaid source transformation currently lives in
`scripts/render_mermaid.py` because it must run before Pandoc reads the book.
Future Pandoc Lua filters belong in this maintainer-owned directory.
